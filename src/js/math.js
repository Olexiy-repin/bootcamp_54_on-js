export const add = (a, b) => {
  return a + b;
};

export const division = (a, b) => {
  return a / b;
};

export const mult = (a, b) => {
  return a * b;
};

export const number = 10;

export default {
  add: add,
  division: division,
  mult: mult,
};
